/**
 * @file Bootstrap test. Verifies Vitest setup is working. Remove once
 * real tests exist.
 */

import { describe, it, expect } from 'vitest';

describe('Bootstrap', () => {
    /**
     * Sanity check test.
     */
    it('should pass', () => {
        expect(true).toBe(true);
    });
});
